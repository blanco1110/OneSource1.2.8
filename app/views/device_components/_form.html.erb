<%= form_with(model: device_component, local: true) do |form| %>
  <% if device_component.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(device_component.errors.count, "error") %> prohibited this device_component from being saved:</h2>

      <ul>
      <% device_component.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :device_version_id, "Device Version" %>
    <%= form.collection_select :device_version_id, DeviceVersion.all, :id, :device_version_name %>
  </div>

  <div class="field">
    <%= form.label :device_comp_name, "Name" %>
    <%= form.text_field :device_comp_name, id: :device_component_device_comp_name %>
  </div>

  <div class="field">
    <%= form.label :device_comp_color, "Color" %>
    <%= form.text_field :device_comp_color, id: :device_component_device_comp_color %>
  </div>

  <div class="field">
    <%= form.label :device_comp_description, "Description" %>
    <%= form.text_field :device_comp_description, id: :device_component_device_comp_description %>
  </div>

  <div class="field">
    <%= form.label :device_component_status_id, "Status" %>
    <%= form.collection_select :device_component_status_id, DeviceComponentStatus.all, :id, :device_comp_status %>
  </div>

  <div class="field">
    <%= form.label :device_comp_cost, "Cost" %>
    <%= form.text_field :device_comp_cost, id: :device_component_device_comp_cost %>
  </div>

    <div class="actions">
      <button type="submit" class="btn btn-outline btl-lg"> Create Component </button>
    </div>
<% end %>
